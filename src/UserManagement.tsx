import React, { useState } from 'react';\nimport { Select, SelectItem } from './Select';\n\nconst UserManagement = () => {\n    const [role, setRole] = useState<'seller' | 'admin' | 'logistics'>('seller');\n    const [shopFields, setShopFields] = useState('');\n\n    const handleCreateUser = () => {\n        if (role === 'logistics') {\n            // Handle logistics user creation without requiring shop fields\n            console.log('Creating logistics user without shop fields');\n        } else {\n            // Handle seller and admin user creation, requiring shop fields\n            console.log('Creating user with shop fields:', shopFields);\n        }\n    };\n\n    return (\n        <div>\n            <Select onChange={setRole}>\n                <SelectItem value='seller'>Seller</SelectItem>\n                <SelectItem value='admin'>Admin</SelectItem>\n                <SelectItem value='logistics'>Logistics</SelectItem>\n            </Select>\n            <button onClick={handleCreateUser}>Create User</button>\n        </div>\n    );\n};\n\nexport default UserManagement;